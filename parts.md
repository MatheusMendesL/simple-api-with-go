# For first, be connected with de db.
# Second, start to configure better the queries into query.sql.go.
# Third, organize the UserDB.go, if it ins´t be more necessary, delete.
# Fourth, organize the response
# Fifth, organize files and paths, on the root only the statics file, like .sum, .mod and .yaml